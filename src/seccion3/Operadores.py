### Ejercicio 2
#**Expresión:**
resultado=8 / 2 + 4 * 3
print(resultado)
#**Explicación:**
# En esta expresión, primero se realiza la división `8 / 2`, que da como resultado `4.0`.
# Luego, se multiplica `4 * 3`, que da como resultado `12`.
# Finalmente, se suman los resultados: `4.0 + 12`, lo que da como resultado `16.0`.

### Ejercicio 3
#**Expresión:**
#`(7 + 3) * 2 - 5`
resultado = (7 + 3) * 2 - 5
print(resultado)
#**Explicación:**
# En esta expresión, primero se realiza la suma `(7 + 3)`, que da como resultado `10`.
# Luego, se multiplica `10 * 2`, que da como resultado `20`.
# Finalmente, se resta `5`, lo que da como resultado `15`.

### Ejercicio 4
#**Expresión:**
#`10 - 4 + 2 * 3`
resultado = 10 - 4 + 2 * 3
print(resultado)
#**Explicación:**
# En esta expresión, primero se realiza la multiplicación `2 * 3`, que da como resultado `6`.
# Luego, se resta `4` de `10`, lo que da como resultado `6

### Ejercicio 5
#**Expresión:**
#`(10 / 2) * (3 + 2) - 4`
resultado = (10 / 2) * (3 + 2) - 4
print(resultado)
#**Explicación:**
# En esta expresión, primero se realiza la división `10 / 2`, que da como resultado `5.0`.
# Luego, se realiza la suma `(3 + 2)`, que da como resultado `5`.
# Finalmente, se multiplica `5.0 * 5`, que da como resultado `25.0`, y se resta `4`, lo que da como resultado `21.0`.

### Ejercicio 6
#**Expresión:**
#`2 + 3 * (4 - 1)`
resultado = 2 + 3 * (4 - 1)
print(resultado)
#**Explicación:**
# En esta expresión, primero se realiza la resta `(4 - 1)`, que da como resultado `3`.
# Luego, se multiplica `3 * 3`, que da como resultado `9`.
# Finalmente, se suma `2 + 9`, lo que da como resultado `11`.

### Ejercicio 7
#**Expresión:**
#`5 * 2 ** 3`
resultado = 5 * 2 ** 3
print(resultado)
#**Explicación:**
# En esta expresión, primero se realiza la potencia `2 ** 3`, que da como resultado `8`.
# Luego, se multiplica `5 * 8`, que da como resultado `40`.

### Ejercicio 8
#**Expresión:**
#`6 + 4 / 2 ** 2`
resultado = 6 + 4 / 2 ** 2
print(resultado)
#**Explicación:**
# En esta expresión, primero se realiza la potencia `2 ** 2`, que da como resultado `4`.
# Luego, se realiza la división `4 / 4`, que da como resultado `1.0`.
# Finalmente, se suma `6 + 1.0`, lo que da como resultado `7.0`.

### Ejercicio 9
#**Expresión:**
#`10 % 3 + 2 * 5`
resultado = 10 % 3 + 2 * 5
print(resultado)
#**Explicación:**
# En esta expresión, primero se realiza el módulo `10 % 3`, que da como resultado `1`.
# Luego, se multiplica `2 * 5`, que da como resultado `10`.
# Finalmente, se suma `1 + 10`, lo que da como resultado `11`.

### Ejercicio 10
#**Expresión:**
#`(8 + 2) * 3 ** 2`
resultado = (8 + 2) * 3 ** 2
print(resultado)
#**Explicación:**
# En esta expresión, primero se realiza la suma `(8 + 2)`, que da como resultado `10`.
# Luego, se realiza la potencia `3 ** 2`, que da como resultado `9
# Finalmente, se multiplica `10 * 9`, lo que da como resultado `90`.

### Ejercicio 11
#**Expresión:**
#`7 + 2 * (3 + 5) / 4`
resultado = 7 + 2 * (3 + 5) / 4
print(resultado)
#**Explicación:**
# En esta expresión, primero se realiza la suma `(3 + 5)`, que da como resultado `8`.
# Luego, se multiplica `2 * 8`, que da como resultado `16`.
# Después, se divide `16 / 4`, que da como resultado `4.0`.
# Finalmente, se suma `7 + 4.0`, lo que da como resultado `11.0`.

### Ejercicio 12
#**Expresión:**
#`2 ** 3 * 4 / 2`
resultado = 2 ** 3 * 4 / 2
print(resultado)
#**Explicación:**
# En esta expresión, primero se realiza la potencia `2 ** 3`, que da como resultado `8`.
# Luego, se multiplica `8 * 4`, que da como resultado `32`.
# Después, se divide `32 / 2`, que da como resultado `16.0`.

### Ejercicio 13
#**Expresión:**
#`9 - 6 + 3 ** 2`
resultado = 9 - 6 + 3 ** 2
print(resultado)
#**Explicación:**
# En esta expresión, primero se realiza la potencia `3 ** 2`, que da como resultado `9`.
# Luego, se resta `6` de `9`, lo que da como resultado `3
# Finalmente, se suma `3 + 9`, lo que da como resultado `12`.

### Ejercicio 14
#**Expresión:**
#`(7 - 2) * 5 + 3 ** 2`
resultado = (7 - 2) * 5 + 3 ** 2
print(resultado)
#**Explicación:**
# En esta expresión, primero se realiza la resta `(7 - 2)`, que da como resultado `5`.
# Luego, se multiplica `5 * 5`, que da como resultado `25`.
# Después, se realiza la potencia `3 ** 2`, que da como resultado `9`.
# Finalmente, se suma `25 + 9`, lo que da como resultado `34`.

### Ejercicio 15
#**Expresión:**
#`4 * 2 ** 3 / 8 + 1`
resultado = 4 * 2 ** 3 / 8 + 1
print(resultado)
#**Explicación:**
# En esta expresión, primero se realiza la potencia `2 ** 3`, que da como resultado `8`.
# Luego, se multiplica `4 * 8`, que da como resultado `32`.
# Después, se divide `32 / 8`, que da como resultado `4.0`.
# Finalmente, se suma `4.0 + 1`, lo que da como resultado `5.0`.